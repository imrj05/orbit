[package]
name = "orbit-rpc"
version = "0.0.16"
description = "Rust client for the pi CLI RPC protocol (pi --mode rpc)"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
