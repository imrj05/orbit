{
  "name": "orbit-guard-bridge",
  "version": "0.0.10",
  "private": true,
  "type": "module",
  "description": "Orbit's access-mode guard: a pi extension loaded with --extension that confirms mutating tool calls according to the active access mode.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  }
}
